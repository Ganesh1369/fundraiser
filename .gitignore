# Documentation (kept local, not version controlled)
docs/

# Production data dumps — contain PII, password hashes, payment IDs, real PANs
db-schema/prod sql/
db-schema/prod-sql/
db-schema/prod_sql/
*.dump.sql

# Local Claude Code agent state
.claude/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local env
.env
.env.local
.env.production

# Local logs
*.log

# E2E test artifacts (local-only, kept out of VCS)
.e2e-*
e2e-*.ps1
seed-test-user.js
