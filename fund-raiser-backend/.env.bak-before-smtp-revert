# Environment Variables
NODE_ENV=development
PORT=4000

# Database Configuration (MariaDB)
DB_HOST=127.0.0.1
DB_PORT=3307
DB_NAME=fundraiser_db
DB_USER=root
DB_PASSWORD=''

# Prisma uses this; keep aligned with the DB_* vars above
DATABASE_URL="mysql://root:@127.0.0.1:3307/fundraiser_db"

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_in_production
JWT_EXPIRES_IN=7d

# Razorpay Configuration
# Live Keys (uncomment for production)
# RAZORPAY_KEY_ID=rzp_live_SFBclItt0gqxAl
# RAZORPAY_KEY_SECRET=zGLEpc4z6pUPiY1AgYCrn4ZQ

# Test Keys (comment out for production)
RAZORPAY_KEY_ID=rzp_test_Rqicgc1XbIpg5j
RAZORPAY_KEY_SECRET=XjfGyG9va8py4rtPyG8hvjda
RAZORPAY_WEBHOOK_SECRET=KEbAj_ULaW4i7FP

# Web Push Configuration (Generate using web-push generate-vapid-keys)
VAPID_PUBLIC_KEY=BBTk9Ux2Wa_U8cgGOA607axPHR9wehKpTmqL9hdm-ZzPY3e7FOdiSP8dSkfkxCLDsvyuDEehcRMfkYCmKfNYWDQ
VAPID_PRIVATE_KEY=nBIaofJ-4XpufoHjT2njkZ_VIK9Qor9MpZQvdW-kqzw
VAPID_SUBJECT=mailto:hello@icenetwork.in

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:4200,http://localhost:3000

# File Upload
MAX_FILE_SIZE=5242880
UPLOAD_DIR=./uploads

# SMTP Configuration (Hostinger)
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=587
SMTP_USER=contact@blackitechs.com
SMTP_PASS="Contact@bits#737"
SMTP_FROM=

# CSR enquiry internal alert inboxes (Workstream 2).
# Both default to SMTP_FROM when unset; split them if Communications gets its own mailbox.
CSR_CONNECT_EMAIL=connect@icenetwork.in
CSR_COMMS_EMAIL=connect@icenetwork.in

# Google reCAPTCHA v3 — the secret key must never reach the frontend.
# Using reCAPTCHA v2 ("I'm not a robot" checkbox): the response is pass/fail, so there
# is no score to threshold. RECAPTCHA_MIN_SCORE is retained only for a future v3 switch.
RECAPTCHA_SECRET_KEY=6LdN5agtAAAAACjf-YhmvAxqcz9YrDT0omEjCwYB
RECAPTCHA_MIN_SCORE=0.5
